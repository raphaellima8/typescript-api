import { testDouble } from '../../config/tests/config/helpers';

describe('Testes unitários no Service User', () => {
    describe('Create Method', () => {
        it('Should return a list of Users', () => {
            const User = { create: testDouble.function() };
            
        });
    });
});
